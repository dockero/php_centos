sudo: required
services:
  - docker

script: 
    - cp .env.example .env
    - sed -i "s/xxxxxx/7.3.12/g" .env
    - docker-compose build
    - sed -i "s/7.3.12/7.4.0/g" .env
    - docker-compose build

after_success:
    - 

branches:
    only:
    - travis
    - master